# ==========================================
# Fleming Production Environment (Lightsail + Caddy)
# ==========================================
# Copy to .env in the deploy/ folder and fill in real values.
# DO NOT commit this file with real secrets.

# ------------------------------------------
# Domain / SSL (Caddy)
# ------------------------------------------
# Use nip.io for free dynamic DNS: <YOUR_IP>.nip.io
# Example: 54.123.45.67.nip.io
DOMAIN_NAME=YOUR_LIGHTSAIL_IP.nip.io
ACME_EMAIL=your-email@example.com

# ------------------------------------------
# Auth
# ------------------------------------------
JWT_SECRET=replace-me-with-a-long-random-secret

# ------------------------------------------
# Database (Neon Postgres)
# ------------------------------------------
# Neon requires SSL. Ensure sslmode=require.
DATABASE_URL=postgresql://user:password@ep-xxxx.region.aws.neon.tech/fleming?sslmode=require

# ------------------------------------------
# Object Storage (Cloudflare R2)
# ------------------------------------------
# Provide the host only (no https://), e.g. <accountid>.r2.cloudflarestorage.com
STORAGE_ENDPOINT=your-account-id.r2.cloudflarestorage.com
STORAGE_ACCESS_KEY=replace-me
STORAGE_SECRET_KEY=replace-me
STORAGE_BUCKET=fleming
